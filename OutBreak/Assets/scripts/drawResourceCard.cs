using UnityEngine;
using System.Collections;

public class drawResourceCard : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	if (situationData .gotogreenSignal ==1)
        {
            print("please draw a new resource card");

        }
    //if the player1 draw recourceCard1
    //Find which card palce is null and give that card to that place

	}
}
